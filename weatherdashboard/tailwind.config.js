/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './weather/templates/weather/**/*.html',
        // Add paths to other apps if necessary
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};